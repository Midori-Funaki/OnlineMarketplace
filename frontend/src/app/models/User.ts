export interface User {
  userId: number;
  firstName: string;
  lastName: string;
  password: string;
  email: string;
  shippingAddress: string;
  billingAddress: string;
  createdAt: string;
  updatedAt: string;
}